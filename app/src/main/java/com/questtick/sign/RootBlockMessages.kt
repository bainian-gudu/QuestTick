package com.questtick.sign

/** Root 阻断相关的统一展示文案，避免弹窗、进度和记录出现不一致。 */
object RootBlockMessages {
    const val DetectedTitle = "检测到Root"
    const val DetectedContent = "检测到Root，已阻断本次签到。"
    const val CheckFailedTitle = "Root检测失败"
    const val CheckFailedContent = "Root检测失败，已阻断本次签到。"
}
